<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.toiletissue.auth.model.dao.MemberMapper">

    <insert id="insertMember" parameterType="com.toiletissue.member.model.dto.MemberSignupDTO">
        INSERT INTO member (
        member_id,
        member_pwd,
        member_name,
        email,
        member_bdate,
        member_gender,
        member_status
        ) VALUES (
        #{memberId},
        #{memberPwd},
        #{memberName},
        #{email},
        #{memberBdate},
        #{memberGender},
        #{memberStatus}
        )
    </insert>

</mapper>

